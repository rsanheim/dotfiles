#!/usr/bin/env bash

sudo apt update
sudo apt install -y ack-grep vim vim-gnome git
# see https://askubuntu.com/questions/773455/what-is-the-correct-way-to-use-git-with-gnome-keyring-and-https-repos
sudo apt install -y libgnome-keyring-dev
sudo make --directory=/usr/share/doc/git/contrib/credential/gnome-keyring

sudo apt install -y curl build-essential
sudo apt install -y autoconf bison libssl-dev libyaml-dev libreadline6-dev zlib1g-dev libncurses5-dev libffi-dev libgdbm3 libgdbm-dev
sudo apt install -y nfs-kernel-server vagrant virtualbox ansible
sudo apt install -y postgresql postgresql-contrib libpq-dev

git clone https://github.com/rbenv/rbenv.git ~/.rbenv
